export const SEVERITY_LEVEL = Object.freeze({
    error: 'error',
    success: 'success',
    warning: 'warning',
    info: 'info',
});
export const GUI_NOTIFICATION_SHOW_TYPE = Object.freeze({
    snackbar: 'snackbar',
});

export const GENDER_OPTIONS = {
    male: 'Male',
    female: 'Female',
    genderqueer: 'Genderqueer',
    other: 'Other',
    transgender: 'Transgender',
};

export const DRUG_SUGGESTION_FILES = ['diagnosis', 'drug_names', 'lab_test', 'symptoms'];
export const SEARCH_METHODS = Object.freeze({
    binary: 'binary',
    linear: 'linear',
});

export const FILTER_TYPES = Object.freeze({
    default: 'default',
    custom: 'custom',
});